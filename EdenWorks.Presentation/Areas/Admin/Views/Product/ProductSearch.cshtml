@model List<ProductVM>
@{
    ViewData["Title"] = "ProductSearch";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}


<div class="row ms-3">
		<div class="col-sm-10">
			<div class="card card-primary">
				<div class="card-header">
					<h3 class="card-title">Product List</h3>
				</div>
				<div class="card-body">
					<table class="table table-hover">
						<thead>
							<tr>
								<th>Id</th>
								<th>Name</th>
								<th>Price</th>
								<th>Description</th>
								<th>Category Name</th>
								<th>Image</th>
								<th>Update</th>
								<th>Delete</th>
							</tr>
						</thead>
						<tbody>
							@foreach (var item in Model)
							{
								<tr>
									<td>@item.Id</td>
									<td>@item.Name</td>
									<td>@item.Price</td>
									<td>@item.Description</td>
									<td>@item.CategoryName</td>
									<td>
										<img src="@item.ImagePath" width"100" height="100">
									</td>
									<td>
										<a asp-action="Update" asp-route-id="@item.Id" class="btn btn-sm btn-warning">Update</a>
									</td>
									<td>
										<a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-danger">Delete</a>
									</td>
								</tr>
							}
						</tbody>
					</table>
				</div>
				<div class="card-footer">
					<a asp-action="List" class="btn btn-sm btn-primary float-end">Product List</a>
				</div>
			</div>
		</div>
	</div>